export const APP_CONFIG = {
  // Set to false to use the native system keyboard instead of the custom on-screen keyboard.
  // Useful for development/testing on a non-touch device.
  virtualKeyboard: true,
};
